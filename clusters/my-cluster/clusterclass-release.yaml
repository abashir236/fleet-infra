---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helm-clusterclass
  namespace: default
spec:
  chart:
    spec:
      chart: templates
      sourceRef:
  interval: 30s
  path: "./clusters/helm-clusterclass"
  prune: true
  sourceRef:
    kind: GitRepository
    name: helm-clusterclass
  targetNamespace: default
  

